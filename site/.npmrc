# Mirrors the repo-root .npmrc (RT-11): npm lifecycle scripts execute BEFORE any
# gate can inspect the tree, so a postinstall is an egress path the allowlist
# verifier never sees. npm reads only the CWD's .npmrc, never an ancestor's, so
# `site/` needs its own copy or `npm --prefix site install` runs unguarded.
ignore-scripts=true
