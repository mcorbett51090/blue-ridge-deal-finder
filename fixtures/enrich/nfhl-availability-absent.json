{
  "_why": "NFHL layer 0 (Availability) with ZERO features — FEMA has not mapped this parcel. Byte-identical in shape to a mapped parcel that is outside every flood polygon, which is exactly why coverage is asked FIRST.",
  "features": []
}
